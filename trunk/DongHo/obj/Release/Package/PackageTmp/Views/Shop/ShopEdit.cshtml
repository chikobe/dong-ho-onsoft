@model DongHo.Models.Shop
@{
    ViewBag.Title = "Thay đổi thông tin cửa hàng";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>@ViewBag.Title</h2>
@using (Html.BeginForm())
{
    <table class="TableUpdate" border="1">
        <tr>
            <td class="Control" colspan="2">
                <ul>
                    @if (Session["Username"]!=null)
                    {
                        <li><input type="submit" value="Ghi lại "class="uupdate" /></li>
                    }
                    else{
                        <li><p class="uupdate" onclick="AlertErr()">Ghi lại</p></li>   
                    }
                    <li>@Html.ActionLink("Trở lại", "ShopIndex","", new { @class="uback"})</li>
                </ul>
            </td>
        </tr>
        <tr>
            <th>Tên cửa hàng</th>
            <td>@Html.TextBoxFor(m=>m.Name, "", new { @class="text"})</td>
        </tr>
        <tr>
            <th>Địa chỉ</th>
            <td>@Html.TextBoxFor(m=>m.Address, "", new { @class="text"})</td>
        </tr>
        <tr>
            <th>Di động</th>
            <td>@Html.TextBoxFor(m=>m.PhoneNumber, "", new { @class="text", @onkeypress="OnlyInputNumber(0)"})</td>
        </tr>
        <tr>
            <td class="Control" colspan="2">
                <ul>
                    @if (Session["Username"]!=null)
                    {
                        <li><input type="submit" value="Ghi lại "class="uupdate" /></li>
                    }
                    else{
                        <li><p class="uupdate" onclick="AlertErr()">Ghi lại</p></li>   
                    }
                    <li>@Html.ActionLink("Trở lại", "ShopIndex","", new { @class="uback"})</li>
                </ul>
            </td>
        </tr>
    </table>
}